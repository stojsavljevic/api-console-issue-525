{
    "name": "api-dashboard",
    "private": true,
    "description": "Used to pull in api-console - proxy for bower",
    "author": "Artur Banas <arturb@ccbilleu.com>",
    "scripts": {
        "postinstall": "bower cache clean --loglevel=error && bower update",
        "build": "npm prune && npm install",
        "compress": "node compress.js"
    },
    "devDependencies": {
        "bower": "1.8.0",
        "bower-art-resolver": "2.0.8"
    },
    "engine": "node >= 6.10.1",
    "dependencies": {
        "api-console-builder": "^0.4.2"
    }
}
